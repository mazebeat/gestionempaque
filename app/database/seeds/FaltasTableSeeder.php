<?php

class FaltasTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('faltas')->truncate();

		$faltas = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('faltas')->insert($faltas);
	}

}
