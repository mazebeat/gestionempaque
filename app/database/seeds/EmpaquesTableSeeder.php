<?php

class EmpaquesTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('empaques')->truncate();

		$empaques = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('empaques')->insert($empaques);
	}

}
