<?php

class AccesosTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('accesos')->truncate();

		$accesos = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('accesos')->insert($accesos);
	}

}
